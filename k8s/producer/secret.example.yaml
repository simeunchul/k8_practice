# 실제 ECOS 수집 시 사용. 복사해서 secret.yaml 로 만들고 키 채운 뒤 apply.
# (secret.yaml 은 .gitignore의 *.local 규칙 밖이므로 커밋 주의 — 직접 빼거나 SealedSecrets 권장)
apiVersion: v1
kind: Secret
metadata:
  name: econ-secrets
type: Opaque
stringData:
  ECOS_API_KEY: "여기에-발급받은-키"
  KOSIS_API_KEY: "여기에-발급받은-키"
